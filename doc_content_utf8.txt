HYDROBOTUI Implementation GuideComponent Library  Â·  Screen Architecture  Â·  Animations  Â·  Code Reference17COMPONENTS8SCREENS5ANIMATIONS2PHASESOverviewThis document covers only the UI implementation â€” Phase 2 (Component Library) and Phase 4 (Screen Implementation) from the master plan. Every component, every screen, every animation is explained in full detail: what to build, how to build it, what props it accepts, and how it connects to live data.Follow the sections in order. Components from Phase 2 are used directly inside the screens in Phase 4, so the component library must be complete before screen work begins.PHASE 2 â€” Component LibraryEvery component in this phase is self-contained, fully typed with TypeScript, reads styles from the design token system (never hardcoded values), and works correctly in both light and dark mode.ATOMSAtomic Base ComponentsButton Â· Text Â· Input Â· Badge Â· SkeletonT-005   Atomic Base ComponentsPriority: CRITICAL      Effort: 3 days      Libraries: react-native-reanimated Â· react-native-haptic-feedback Â· @shopify/restyle1.1  Button ComponentThe Button is the most-used atom in the app. It has 6 visual variants and a consistent press animation applied to every variant.The 6 Button Variantsprimary â€” solid fill with brand color. Used for main actions like Start Mission and Confirm.secondary â€” outlined border, transparent fill. Used for secondary actions.ghost â€” no border, no fill. Just text. Used inside cards and lists for inline actions.danger â€” solid red fill. Used for destructive actions like Delete and Emergency Stop.icon â€” square or circular, icon only. Used in toolbars and map overlays.fab â€” floating action button. Large circular with shadow elevation. Positioned absolutely.Press AnimationAll 6 variants share the same Reanimated spring press animation. When the user presses the button, it scales down to 0.96. When released, it springs back to 1.0. This gives tactile feedback.// src/components/atoms/Button.tsximport Animated, { useSharedValue, useAnimatedStyle,  withSpring } from &apos;react-native-reanimated&apos;;import { Pressable } from &apos;react-native&apos;;import HapticFeedback from &apos;react-native-haptic-feedback&apos;; export function Button({ variant = &apos;primary&apos;, onPress, children, ...props }) {  const scale = useSharedValue(1);   const animStyle = useAnimatedStyle(() =&gt; ({    transform: [{ scale: scale.value }],  }));   function handlePressIn() {    scale.value = withSpring(0.96, { damping: 15, stiffness: 300 });    HapticFeedback.trigger(&apos;impactLight&apos;);  }  function handlePressOut() {    scale.value = withSpring(1.0, { damping: 12, stiffness: 200 });  }   return (    &lt;Animated.View style={animStyle}&gt;      &lt;Pressable onPressIn={handlePressIn} onPressOut={handlePressOut}        onPress={onPress} style={styles[variant]} {...props}&gt;        {children}      &lt;/Pressable&gt;    &lt;/Animated.View&gt;  );}Button PropsProp / StateType / ValuePurposevariant&apos;primary&apos;|&apos;secondary&apos;|&apos;ghost&apos;|&apos;danger&apos;|&apos;icon&apos;|&apos;fab&apos;Controls visual styleonPress() =&gt; voidCallback when button is tappeddisabledbooleanReduces opacity to 0.4, blocks press eventsloadingbooleanShows ActivityIndicator, blocks press eventsleftIconReactNodeIcon displayed left of label textfullWidthbooleanStretches button to fill container width1.2  Text ComponentA single Text component replaces all raw &lt;Text&gt; usage in the app. It enforces consistent font sizes, weights, and colors from the theme.The 5 Text Variantsheading â€” 32px, bold, primary text color. Used for screen titles.subheading â€” 24px, semi-bold. Used for section titles within a screen.body â€” 16px, regular weight. Default body copy.caption â€” 13px, regular. Used for timestamps, labels, and secondary info.mono â€” 14px, JetBrains Mono font. Used for sensor values, IDs, and code-like data.// src/components/atoms/Text.tsximport { Text as RNText } from &apos;react-native&apos;;import { useTheme } from &apos;@shopify/restyle&apos;; const variantStyles = {  heading:    { fontSize: 32, fontWeight: &apos;700&apos; },  subheading: { fontSize: 24, fontWeight: &apos;600&apos; },  body:       { fontSize: 16, fontWeight: &apos;400&apos; },  caption:    { fontSize: 13, fontWeight: &apos;400&apos; },  mono:       { fontSize: 14, fontFamily: &apos;JetBrainsMono-Regular&apos; },}; export function Text({ variant = &apos;body&apos;, color, style, children }) {  const theme = useTheme();  const textColor = color ? theme.colors[color] : theme.colors.text;  return (    &lt;RNText style={[variantStyles[variant], { color: textColor }, style]}&gt;      {children}    &lt;/RNText&gt;  );}1.3  Input ComponentThe Input component wraps TextInput with consistent styling, a label above, and built-in validation error display below.Three Input Typestext â€” default. Accepts any string. Shows a keyboard with all characters.numeric â€” shows number pad. Used for threshold values and port numbers.search â€” shows a magnifying glass icon on the left, a clear button on the right when text is present.Validation Statesidle â€” neutral border from theme tokens.focused â€” border changes to primary color, slight glow shadow.error â€” border turns red, error message appears below the input in caption size.success â€” border turns green. Used after successful validation.// src/components/atoms/Input.tsxexport function Input({ type = &apos;text&apos;, label, error, value, onChangeText }) {  const [focused, setFocused] = useState(false);   const borderColor = error ? colors.danger    : focused ? colors.primary : colors.border;   return (    &lt;View&gt;      {label &amp;&amp; &lt;Text variant=&apos;caption&apos;&gt;{label}&lt;/Text&gt;}      &lt;TextInput        value={value}        onChangeText={onChangeText}        onFocus={() =&gt; setFocused(true)}        onBlur={() =&gt; setFocused(false)}        keyboardType={type === &apos;numeric&apos; ? &apos;numeric&apos; : &apos;default&apos;}        style={[styles.input, { borderColor }]}      /&gt;      {error &amp;&amp; &lt;Text variant=&apos;caption&apos; color=&apos;danger&apos;&gt;{error}&lt;/Text&gt;}    &lt;/View&gt;  );}1.4  Badge ComponentA small pill-shaped label used to display status, counts, and categories throughout the app.4 colour variants: success (green), warning (orange), danger (red), info (blue).2 size variants: small (used in lists and table rows) and large (used on cards).Optional left dot â€” a small filled circle matching the variant colour. Used for status indicators.// Usage examples&lt;Badge variant=&apos;danger&apos; size=&apos;small&apos;&gt;CRITICAL&lt;/Badge&gt;&lt;Badge variant=&apos;success&apos; size=&apos;large&apos; showDot&gt;ONLINE&lt;/Badge&gt;&lt;Badge variant=&apos;warning&apos;&gt;LOW BATTERY&lt;/Badge&gt;1.5  Skeleton LoaderThe Skeleton component shows an animated shimmer placeholder while data is loading. It must be placed in the exact position the real content will occupy so the layout does not shift when data arrives.The shimmer is a gradient that moves from left to right on a loop using Reanimated&apos;s withRepeat and withTiming. The gradient goes from the background colour to a slightly lighter shade and back.// src/components/atoms/Skeleton.tsximport Animated, { useSharedValue, useAnimatedStyle,  withRepeat, withTiming } from &apos;react-native-reanimated&apos;; export function Skeleton({ width, height, borderRadius = 8 }) {  const opacity = useSharedValue(0.4);   useEffect(() =&gt; {    opacity.value = withRepeat(      withTiming(1, { duration: 800 }),      -1,   // repeat forever      true  // reverse each cycle    );  }, []);   const style = useAnimatedStyle(() =&gt; ({ opacity: opacity.value }));   return (    &lt;Animated.View style={[{ width, height, borderRadius,      backgroundColor: colors.skeletonBase }, style]} /&gt;  );}MOLECULESRobot Status CardRobotStatusCard Â· BatteryGauge Â· ConnectionPulseT-006   Robot Status CardPriority: CRITICAL      Effort: 2 days      Libraries: react-native-svg Â· react-native-reanimated Â· lottie-react-native2.1  BatteryGauge â€” SVG Arc GaugeThe BatteryGauge draws an SVG arc using react-native-svg. The arc sweeps from the bottom-left to the bottom-right of a circle, covering 240 degrees total. The filled portion of the arc represents battery percentage.How the arc is calculatedThe total arc covers 240 degrees. Start angle is 150 degrees (bottom-left). End angle is 30 degrees (bottom-right going clockwise).Filled arc length = batteryPercent / 100 Ã— 240 degrees.Convert degrees to SVG path coordinates using polar-to-cartesian conversion.Draw two arcs: a grey background track (full 240 degrees) and a coloured foreground arc (battery-percent degrees).Foreground arc colour changes: green above 50%, orange between 20% and 50%, red below 20%.// src/components/robot/BatteryGauge.tsximport { Svg, Path, Text as SvgText } from &apos;react-native-svg&apos;; function polarToCartesian(cx, cy, r, angleDeg) {  const rad = ((angleDeg - 90) * Math.PI) / 180;  return { x: cx + r * Math.cos(rad), y: cy + r * Math.sin(rad) };} function arcPath(cx, cy, r, startAngle, endAngle) {  const s = polarToCartesian(cx, cy, r, startAngle);  const e = polarToCartesian(cx, cy, r, endAngle);  const large = endAngle - startAngle &gt; 180 ? 1 : 0;  return `M ${s.x} ${s.y} A ${r} ${r} 0 ${large} 1 ${e.x} ${e.y}`;} export function BatteryGauge({ percent, size = 80 }) {  const cx = size / 2, cy = size / 2, r = size * 0.38;  const filled = (percent / 100) * 240;  const color = percent &gt; 50 ? &apos;#34D399&apos; : percent &gt; 20 ? &apos;#FFA94D&apos; : &apos;#FF6B6B&apos;;   return (    &lt;Svg width={size} height={size}&gt;      &lt;Path d={arcPath(cx,cy,r,150,30)} stroke=&apos;#334155&apos; strokeWidth={8} fill=&apos;none&apos;/&gt;      &lt;Path d={arcPath(cx,cy,r,150,150+filled)} stroke={color}        strokeWidth={8} fill=&apos;none&apos; strokeLinecap=&apos;round&apos;/&gt;      &lt;SvgText x={cx} y={cy+6} textAnchor=&apos;middle&apos; fontSize={14}        fill={color} fontWeight=&apos;bold&apos;&gt;{percent}%&lt;/SvgText&gt;    &lt;/Svg&gt;  );}2.2  ConnectionPulse â€” Animated Status DotA small dot that pulses continuously when the robot is connected, indicating a live connection. When disconnected, it shows as a solid red dot with no animation.// src/components/robot/ConnectionPulse.tsximport Animated, { useSharedValue, useAnimatedStyle,  withRepeat, withTiming, withSequence } from &apos;react-native-reanimated&apos;; export function ConnectionPulse({ connected }) {  const scale = useSharedValue(1);  const opacity = useSharedValue(1);   useEffect(() =&gt; {    if (connected) {      scale.value = withRepeat(withSequence(        withTiming(1.6, { duration: 700 }),        withTiming(1.0, { duration: 700 })      ), -1);      opacity.value = withRepeat(withSequence(        withTiming(0.3, { duration: 700 }),        withTiming(1.0, { duration: 700 })      ), -1);    } else {      scale.value = 1;  // cancel animation      opacity.value = 1;    }  }, [connected]);   const dotStyle = useAnimatedStyle(() =&gt; ({    transform: [{ scale: scale.value }],    opacity: opacity.value,  }));   return (    &lt;Animated.View style={[styles.dot,      { backgroundColor: connected ? &apos;#34D399&apos; : &apos;#FF6B6B&apos; }, dotStyle]} /&gt;  );}2.3  RobotStatusCard â€” Full Card ComponentThe main card displayed in the Dashboard and Fleet screens. It composes BatteryGauge, ConnectionPulse, and displays mission status.Card Layout â€” top to bottom:Row 1  â€” Robot name (bold) + ConnectionPulse on the rightRow 2  â€” BatteryGauge (left, 80px) + Mission status text (right)Row 3  â€” Last seen timestamp in caption styleSkeleton  â€” Grey shimmer replacing all rows when isLoading=trueEntrance AnimationWhen the card mounts, it animates in using Reanimated&apos;s FadeInDown built-in entering animation. Cards in a list are staggered: each card delays 60ms more than the previous one.// src/components/robot/RobotStatusCard.tsximport Animated, { FadeInDown } from &apos;react-native-reanimated&apos;; export function RobotStatusCard({ robot, index, isLoading }) {  if (isLoading) {    return (      &lt;View style={styles.card}&gt;        &lt;Skeleton width=&apos;60%&apos; height={20} /&gt;        &lt;Skeleton width=&apos;100%&apos; height={60} /&gt;        &lt;Skeleton width=&apos;40%&apos; height={14} /&gt;      &lt;/View&gt;    );  }   return (    &lt;Animated.View      entering={FadeInDown.delay(index * 60).springify()}      style={styles.card}    &gt;      &lt;View style={styles.row}&gt;        &lt;Text variant=&apos;subheading&apos;&gt;{robot.name}&lt;/Text&gt;        &lt;ConnectionPulse connected={robot.isOnline} /&gt;      &lt;/View&gt;      &lt;View style={styles.row}&gt;        &lt;BatteryGauge percent={robot.battery} size={80} /&gt;        &lt;View&gt;          &lt;Text variant=&apos;caption&apos;&gt;Mission&lt;/Text&gt;          &lt;Text variant=&apos;body&apos;&gt;{robot.missionStatus}&lt;/Text&gt;        &lt;/View&gt;      &lt;/View&gt;      &lt;Text variant=&apos;caption&apos; color=&apos;textMuted&apos;&gt;        Last seen: {formatDistanceToNow(robot.lastSeen)} ago      &lt;/Text&gt;    &lt;/Animated.View&gt;  );}CHARTSSensor Data ChartsLineChart Â· AreaChart Â· GaugeChart Â· TimeRangeSelectorT-007   Sensor Data ChartsPriority: CRITICAL      Effort: 4 days      Libraries: victory-native-xl Â· @shopify/react-native-skia Â· react-native-gesture-handler3.1  Why Victory Native XLVictory Native XL renders charts directly using React Native Skia. This means the chart draws on the UI thread using native graphics â€” it never goes through the JavaScript bridge. The result is that charts with 500+ data points scroll and zoom at 60fps without any jank.IMPORTANT: Do NOT use recharts, Victory Native (original), or React Native Chart Kit. These render via the JS bridge and will cause dropped frames when displaying live sensor data.3.2  SensorLineChart â€” pH TrendDisplays pH readings over time as a line chart. pH is the most critical sensor â€” it should always be visible at the top of the Analytics screen.Visual design:Line colour: cyan (#00E5FF) â€” matches the brand palette.Area fill: gradient from cyan at 30% opacity at the top fading to 0% at the bottom.Threshold lines: dashed horizontal lines at pH 6.5 (lower safe limit) and pH 8.5 (upper safe limit).Data point dots: only shown for the most recent 5 points. Hidden for older points to reduce clutter.Y axis: labelled 0â€“14 (full pH range). Auto-scales to show the visible data range with 10% padding.X axis: shows time labels formatted by the selected time range (HH:mm for 1H/6H, MM/DD for 7D).// src/components/charts/SensorLineChart.tsximport { CartesianChart, Line, Area, useChartPressState }  from &apos;victory-native&apos;;import { useFont } from &apos;@shopify/react-native-skia&apos;; export function SensorLineChart({ data, thresholdLow = 6.5, thresholdHigh = 8.5 }) {  const font = useFont(require(&apos;@/assets/fonts/JetBrainsMono.ttf&apos;), 11);  const { state, isActive } = useChartPressState({ x: 0, y: { value: 0 } });   return (    &lt;CartesianChart      data={data}      xKey=&apos;timestamp&apos;      yKeys={[&apos;value&apos;]}      axisOptions={{ font, labelColor: &apos;#64748B&apos;, lineColor: &apos;#1E3A5F&apos; }}      chartPressState={state}    &gt;      {({ points, chartBounds }) =&gt; (        &lt;&gt;          {/* Gradient fill area under line */}          &lt;Area points={points.value} y0={chartBounds.bottom}            color=&apos;#00E5FF&apos; opacity={0.15} animate={{ type: &apos;spring&apos; }} /&gt;           {/* Main line */}          &lt;Line points={points.value} color=&apos;#00E5FF&apos;            strokeWidth={2} animate={{ type: &apos;spring&apos; }} /&gt;           {/* Crosshair tooltip when pressed */}          {isActive &amp;&amp; &lt;Tooltip state={state} font={font} /&gt;}        &lt;/&gt;      )}    &lt;/CartesianChart&gt;  );}3.3  GaugeChart â€” Real-Time TemperatureA circular gauge showing the current temperature value. Unlike the line chart, this updates in real time (every 500ms) and always shows a single current value rather than history.Implementation approach:Draw the gauge background arc using react-native-svg (same technique as BatteryGauge).The filled arc represents the value on a 0â€“50Â°C scale for water temperature.The arc colour interpolates: blue (cold) â†’ green (normal 20-30Â°C) â†’ red (hot).Animate the arc length change using Reanimated&apos;s withSpring when the value updates.Display the numeric value and unit (Â°C) in the centre using SVG Text.3.4  TimeRangeSelectorA horizontal row of 4 buttons â€” 1H, 6H, 24H, 7D â€” that controls which time window is shown on all charts simultaneously. When a range button is selected, it fetches new historical data via the useSensorHistory hook.Behaviour:Selected button: filled primary colour background, white text.Unselected: transparent background, muted text colour.Switching range triggers a loading skeleton on all 4 charts simultaneously.The selected range is stored in the uiStore so it persists if the user navigates away and back.// src/components/charts/TimeRangeSelector.tsxconst RANGES = [&apos;1H&apos;, &apos;6H&apos;, &apos;24H&apos;, &apos;7D&apos;] as const; export function TimeRangeSelector({ selected, onChange }) {  return (    &lt;View style={styles.row}&gt;      {RANGES.map(range =&gt; (        &lt;Pressable key={range} onPress={() =&gt; onChange(range)}          style={[styles.btn, selected === range &amp;&amp; styles.btnActive]}&gt;          &lt;Text style={selected === range ? styles.textActive : styles.text}&gt;            {range}          &lt;/Text&gt;        &lt;/Pressable&gt;      ))}    &lt;/View&gt;  );}CONTROLSHaptic Joystick ControllerJoystick Â· SpeedSlider Â· DirectionIndicatorT-008   Haptic Joystick ControllerPriority: HIGH      Effort: 3 days      Libraries: react-native-gesture-handler Â· react-native-reanimated Â· react-native-haptic-feedback4.1  Joystick GeometryThe joystick consists of two circles: a large outer ring (240px diameter) and a small inner thumb (60px). The outer ring stays fixed. The thumb moves within it.Movement constraints:The thumb&apos;s centre point is constrained to stay within a 100px radius of the joystick centre. This is calculated using: if (distance &gt; MAX_RADIUS) clamp to the edge of the circle.Dead zone: a 20px radius around the centre is treated as neutral. This prevents micro-movements from sending commands.Optional snap mode: the movement angle is rounded to the nearest 45 degrees (8 directions). This helps when driving in straight lines.// src/components/controls/Joystick.tsximport { Gesture, GestureDetector } from &apos;react-native-gesture-handler&apos;;import Animated, { useSharedValue, useAnimatedStyle,  withSpring, runOnJS } from &apos;react-native-reanimated&apos;; const MAX_RADIUS = 100;const DEAD_ZONE  = 20; export function Joystick({ onCommand, snapMode = false }) {  const thumbX = useSharedValue(0);  const thumbY = useSharedValue(0);   // Convert cartesian to command values (-1 to +1)  function emitCommand(x, y) {    const dist = Math.sqrt(x*x + y*y);    if (dist &lt; DEAD_ZONE) { onCommand({ x: 0, y: 0 }); return; }    onCommand({      x: parseFloat((x / MAX_RADIUS).toFixed(2)),      y: parseFloat((y / MAX_RADIUS).toFixed(2)),    });  }   const gesture = Gesture.Pan()    .onUpdate(e =&gt; {      const dist = Math.sqrt(e.translationX**2 + e.translationY**2);      const clamped = Math.min(dist, MAX_RADIUS);      const angle   = Math.atan2(e.translationY, e.translationX);      thumbX.value = clamped * Math.cos(angle);      thumbY.value = clamped * Math.sin(angle);      runOnJS(emitCommand)(thumbX.value, thumbY.value);    })    .onEnd(() =&gt; {      // Spring back to centre when finger lifts      thumbX.value = withSpring(0, { damping: 15 });      thumbY.value = withSpring(0, { damping: 15 });      runOnJS(onCommand)({ x: 0, y: 0 });    });   const thumbStyle = useAnimatedStyle(() =&gt; ({    transform: [{ translateX: thumbX.value }, { translateY: thumbY.value }]  }));   return (    &lt;GestureDetector gesture={gesture}&gt;      &lt;View style={styles.outerRing}&gt;        &lt;Animated.View style={[styles.thumb, thumbStyle]} /&gt;      &lt;/View&gt;    &lt;/GestureDetector&gt;  );}4.2  Command Emission RateThe joystick emits commands to the WebSocket at 20Hz (every 50ms). This is achieved using a useInterval hook inside the Control screen â€” not inside the gesture handler itself. The interval reads the latest thumbX and thumbY shared values and sends the command.IMPORTANT: Do not call WebSocket send() directly inside the gesture onUpdate callback. At full joystick movement, onUpdate can fire 60 times per second, which would flood the robot. Use a 20Hz interval instead.// Inside ControlScreen â€” 20Hz command loopconst thumbX = useRef(0);   // updated by joystick callbackconst thumbY = useRef(0); // The joystick just updates these refs:function handleJoystickCommand({ x, y }) {  thumbX.current = x;  thumbY.current = y;} // The interval sends commands at a controlled rate:useEffect(() =&gt; {  const interval = setInterval(() =&gt; {    wsService.send(&apos;robot:drive&apos;, {      x: thumbX.current,      y: thumbY.current,      robotId: selectedRobotId,    });  }, 50); // 20Hz  return () =&gt; clearInterval(interval);}, [selectedRobotId]);NOTIFICATIONSToast &amp; Alert ComponentsToast Â· AlertItem Â· AlertDrawerT-009   Toast Notifications &amp; Alert ComponentsPriority: HIGH      Effort: 2 days      Libraries: react-native-toast-message Â· react-native-reanimated5.1  Toast ConfigurationConfigure 4 custom toast types in the App root. Each type has a custom component that renders the toast with the correct colour and icon.Prop / StateType / ValuePurposecriticalRed background, warning iconLow battery, obstacle detected, sensor failurewarningOrange background, caution iconSensor drift, signal weak, trash bin near fullinfoBlue background, info iconMission waypoint reached, sync completesuccessGreen background, check iconMission complete, robot docked, calibration doneToast behaviour:Slides in from the top of the screen in 200ms using a spring animation.Auto-dismisses after 4 seconds. A thin progress bar below the message depletes in real time.User can swipe up to dismiss immediately â€” the swipe triggers a spring exit animation upward.Critical toasts do NOT auto-dismiss. They stay until the user manually swipes them away.// src/components/notifications/Toast.tsximport Toast, { BaseToast } from &apos;react-native-toast-message&apos;; export const toastConfig = {  critical: (props) =&gt; (    &lt;BaseToast {...props}      style={{ borderLeftColor: &apos;#FF6B6B&apos;, backgroundColor: &apos;#1A0505&apos; }}      text1Style={{ color: &apos;#FF6B6B&apos;, fontWeight: &apos;bold&apos; }}      text2Style={{ color: &apos;#AAAAAA&apos; }}    /&gt;  ),  warning: (props) =&gt; (    &lt;BaseToast {...props}      style={{ borderLeftColor: &apos;#FFA94D&apos;, backgroundColor: &apos;#1A0D05&apos; }}      text1Style={{ color: &apos;#FFA94D&apos;, fontWeight: &apos;bold&apos; }}    /&gt;  ),  // ... info and success variants follow the same pattern}; // Show a toast from anywhere:Toast.show({ type: &apos;critical&apos;, text1: &apos;Low Battery&apos;, text2: &apos;Robot #2 at 12%&apos; });5.2  AlertItem â€” List Row ComponentEach row in the Alerts screen is an AlertItem. It supports swipe-to-dismiss using Gesture Handler&apos;s Swipeable component.Row layout â€” left to right:Severity dot  â€” 8px circle, colour matches severity levelContent area  â€” Alert message (body), robot ID + timestamp (caption)Unread badge  â€” Blue dot, only shown for unread alertsSwipe delete  â€” Red background revealed on left swipe, trash iconPHASE 4 â€” Screen ImplementationEach screen is a composition of Phase 2 components wired to live data from the Zustand stores and services built in Phase 3. Build screens in the order listed â€” each one introduces complexity that prepares you for the next.SCREEN 1Dashboard ScreenHome screen Â· Overview Â· Quick ActionsT-014   Dashboard ScreenPriority: CRITICAL      Effort: 3 days      Libraries: @shopify/flash-list Â· react-native-linear-gradient Â· react-native-reanimated6.1  Screen LayoutHeader bar  â€” App name + notification bell + connection dot (fixed, not scrollable)Robot Status Grid  â€” 2-column FlashList of RobotStatusCard componentsMission Summary Card  â€” Today&apos;s active mission: distance, trash collected, time elapsedWater Quality Index  â€” Circular ring chart, composite score 0â€“100, colour codedRecent Alerts Strip  â€” Horizontal scroll of last 5 AlertItem rowsFAB Menu  â€” Floating button (bottom-right), expands to 3 action buttons6.2  Robot Status GridThe robot grid uses FlashList in a 2-column layout. Each cell renders one RobotStatusCard with the staggered entrance animation (60ms delay per index).// src/screens/Dashboard/DashboardScreen.tsximport { FlashList } from &apos;@shopify/flash-list&apos;; function RobotGrid() {  const robots = useRobotStore(s =&gt; s.robots);   return (    &lt;FlashList      data={robots}      numColumns={2}      estimatedItemSize={160}      keyExtractor={r =&gt; r.id}      renderItem={({ item, index }) =&gt; (        &lt;RobotStatusCard robot={item} index={index} /&gt;      )}    /&gt;  );}6.3  FAB Expand AnimationThe FAB starts as a single cyan circle with a + icon. When tapped, it expands to reveal 3 child action buttons that fan out above it. The + icon rotates 45 degrees to become an X.Animation sequence when FAB is tapped:The + icon rotates from 0 to 45 degrees using withSpring.Three child buttons animate in from the FAB position, each with a different vertical offset (72px, 144px, 216px above the FAB).Child buttons use FadeIn + SlideInUp with staggered delays: 0ms, 50ms, 100ms.A semi-transparent backdrop fades in behind the buttons to indicate the FAB is active.// src/screens/Dashboard/FABMenu.tsxconst ACTIONS = [  { icon: &apos;play&apos;, label: &apos;Start Mission&apos;, onPress: startMission },  { icon: &apos;navigation&apos;, label: &apos;Return Home&apos;, onPress: returnHome },  { icon: &apos;alert-octagon&apos;, label: &apos;Emergency Stop&apos;, onPress: emergencyStop, color: &apos;danger&apos; },]; export function FABMenu() {  const [open, setOpen] = useState(false);  const rotation = useSharedValue(0);   function toggle() {    setOpen(v =&gt; !v);    rotation.value = withSpring(open ? 0 : 45);  }   return (    &lt;View style={styles.fabContainer}&gt;      {open &amp;&amp; ACTIONS.map((action, i) =&gt; (        &lt;Animated.View key={action.label}          entering={FadeInDown.delay(i * 50).springify()}          exiting={FadeOutDown.delay(i * 30)}          style={[styles.childBtn, { bottom: (i + 1) * 72 }]}        &gt;          &lt;Button variant=&apos;fab&apos; onPress={action.onPress}&gt;{action.icon}&lt;/Button&gt;        &lt;/Animated.View&gt;      ))}      &lt;Button variant=&apos;fab&apos; onPress={toggle}&gt;        &lt;Animated.Text style={[styles.plus, rotationStyle]}&gt;+&lt;/Animated.Text&gt;      &lt;/Button&gt;    &lt;/View&gt;  );}SCREEN 2Robot Control ScreenJoystick Â· Telemetry Â· Mode Selector Â· Emergency StopT-015   Robot Control ScreenPriority: CRITICAL      Effort: 4 days      Libraries: expo-keep-awake Â· react-native-reanimated Â· react-native-gesture-handler7.1  Screen LayoutTop bar  â€” Connection badge (left) + Mode selector (centre) + Emergency STOP button (right)Telemetry strip  â€” 4 live values: Speed (km/h) | Heading (Â°) | Power (%) | Latency (ms)Main control area  â€” Left half: Joystick component  |  Right half: Speed Slider (vertical)Status footer  â€” Battery level + GPS coordinates + mission mode label7.2  Telemetry StripA horizontal row of 4 data cells that updates every 500ms from the robotStore. Each cell has a label above and a value below in monospace font. Values animate when they change using Reanimated&apos;s withTiming on text opacity.// src/screens/Control/TelemetryStrip.tsxfunction TelemetryCell({ label, value, unit }) {  return (    &lt;View style={styles.cell}&gt;      &lt;Text variant=&apos;caption&apos; color=&apos;textMuted&apos;&gt;{label}&lt;/Text&gt;      &lt;Text variant=&apos;mono&apos; color=&apos;primary&apos;&gt;        {value}&lt;Text variant=&apos;caption&apos;&gt; {unit}&lt;/Text&gt;      &lt;/Text&gt;    &lt;/View&gt;  );} export function TelemetryStrip() {  const { speed, heading, power, latency } = useRobotStore(s =&gt; s.telemetry);  return (    &lt;View style={styles.strip}&gt;      &lt;TelemetryCell label=&apos;SPEED&apos;   value={speed.toFixed(1)} unit=&apos;km/h&apos; /&gt;      &lt;TelemetryCell label=&apos;HEADING&apos; value={heading}          unit=&apos;deg&apos;  /&gt;      &lt;TelemetryCell label=&apos;POWER&apos;   value={power}            unit=&apos;%&apos;    /&gt;      &lt;TelemetryCell label=&apos;LATENCY&apos; value={latency}          unit=&apos;ms&apos;   /&gt;    &lt;/View&gt;  );}7.3  Emergency Stop ButtonThe Emergency Stop button requires a 2-second press and hold to activate. This prevents accidental activation while the user is operating the joystick.Hold-to-activate mechanics:User presses and holds the STOP button.A circular progress animation starts â€” a Skia arc sweeps from 0 to 360 degrees over 2 seconds.If the user holds for the full 2 seconds, the button fires the emergency stop command, plays a critical haptic, and the joystick goes inactive.If the user releases before 2 seconds, the progress resets with a spring animation.// src/screens/Control/EmergencyStopButton.tsxexport function EmergencyStopButton({ onActivate }) {  const progress = useSharedValue(0);   // 0 to 1  const holdTimer = useRef(null);   function onPressIn() {    progress.value = withTiming(1, { duration: 2000 });    holdTimer.current = setTimeout(() =&gt; {      HapticFeedback.trigger(&apos;notificationError&apos;);      onActivate();    }, 2000);  }   function onPressOut() {    clearTimeout(holdTimer.current);    progress.value = withSpring(0);  }   // Skia arc drawn as progress fills  return (    &lt;Pressable onPressIn={onPressIn} onPressOut={onPressOut} style={styles.btn}&gt;      &lt;ProgressArc progress={progress} color=&apos;#FF6B6B&apos; /&gt;      &lt;Text style={styles.label}&gt;STOP&lt;/Text&gt;    &lt;/Pressable&gt;  );}SCREEN 3Water Analytics ScreenSensor Charts Â· Thresholds Â· Pollution Index Â· ExportT-016   Water Analytics ScreenPriority: HIGH      Effort: 3 days      Libraries: victory-native-xl Â· @gorhom/bottom-sheet Â· react-native-share8.1  Screen LayoutPollution Index card  â€” Composite score 0â€“100, ring chart, colour coded, always at topTimeRangeSelector  â€” Sticky below header as user scrolls the chart panelspH Panel  â€” SensorLineChart + current value badge + status badge + threshold tap targetTurbidity Panel  â€” SensorAreaChart + current value + status badgeTemperature Panel  â€” GaugeChart (large) + 24h sparkline beside it + current valueTDS Panel  â€” SensorBarChart (daily averages) + current value + status badgeExport button  â€” Fixed at screen bottom, always visible, does not scroll8.2  Pollution Index CalculationThe Pollution Index is a single composite score from 0 (clean) to 100 (heavily polluted) computed from all 4 sensor readings. It is recalculated every time any sensor updates.// src/utils/pollutionIndex.tsexport function calcPollutionIndex(sensors) {  // Normalise each sensor to 0-100 score where 100 = worst  const phScore = Math.abs(sensors.ph - 7.0) / 7.0 * 100;  // ideal pH = 7  const turbScore  = Math.min(sensors.turbidity / 100, 1) * 100;  const tempScore  = Math.max(sensors.temperature - 25, 0) / 25 * 100;  const tdsScore   = Math.min(sensors.tds / 500, 1) * 100;   // Weighted average â€” pH and turbidity weighted higher  return Math.round(    phScore * 0.35 + turbScore * 0.35 + tempScore * 0.15 + tdsScore * 0.15  );}8.3  Threshold Bottom SheetTapping the status badge on any sensor panel opens a bottom sheet from @gorhom/bottom-sheet. The sheet contains two numeric input fields: Caution threshold and Critical threshold for that sensor. Saving writes the new values to the sensorStore and persists to MMKV.// src/screens/Analytics/ThresholdSheet.tsximport BottomSheet, { BottomSheetView } from &apos;@gorhom/bottom-sheet&apos;; export function ThresholdSheet({ sensor, onClose }) {  const sheetRef = useRef(null);  const [caution, setCaution] = useState(sensor.thresholds.caution.toString());  const [critical, setCritical] = useState(sensor.thresholds.critical.toString());   function handleSave() {    useSensorStore.getState().setThresholds(sensor.type, {      caution: parseFloat(caution),      critical: parseFloat(critical),    });    sheetRef.current?.close();  }   return (    &lt;BottomSheet ref={sheetRef} snapPoints={[&apos;40%&apos;]} onClose={onClose}&gt;      &lt;BottomSheetView&gt;        &lt;Text variant=&apos;subheading&apos;&gt;{sensor.label} Thresholds&lt;/Text&gt;        &lt;Input label=&apos;Caution level&apos; type=&apos;numeric&apos; value={caution} onChangeText={setCaution} /&gt;        &lt;Input label=&apos;Critical level&apos; type=&apos;numeric&apos; value={critical} onChangeText={setCritical} /&gt;        &lt;Button variant=&apos;primary&apos; onPress={handleSave}&gt;Save Thresholds&lt;/Button&gt;      &lt;/BottomSheetView&gt;    &lt;/BottomSheet&gt;  );}SCREEN 4Live Map &amp; GPS TrackingRobot Tracking Â· Route Trail Â· Coverage Â· Mission ZoneT-017   Live Map &amp; GPS Tracking ScreenPriority: HIGH      Effort: 4 days      Libraries: react-native-maps Â· react-native-geolocation-service Â· @turf/turf9.1  Map ConfigurationUse react-native-maps with Google Maps provider. Default to dark satellite style for professional appearance. The map is full-screen with overlay UI elements positioned absolutely on top.Overlay elements on the map:Top-left: map style toggle button (satellite / standard / dark).Top-right: info panel â€” robot battery, speed, and GPS accuracy.Bottom-right: zoom to robot button (flying camera to robot position).Bottom-left: coverage area badge showing mÂ² cleaned.Bottom sheet that slides up to show mission controls.9.2  Robot Marker â€” Animated Heading ArrowThe robot position is shown as a custom marker. The marker has a circular background and a directional arrow that rotates to match the robot&apos;s heading. When the heading changes, the arrow rotates smoothly using interpolation rather than jumping.// src/screens/Map/RobotMarker.tsxexport function RobotMarker({ coordinate, heading }) {  const rotation = useSharedValue(heading);   useEffect(() =&gt; {    // Animate to new heading, taking the shortest rotation path    rotation.value = withTiming(heading, { duration: 300 });  }, [heading]);   const arrowStyle = useAnimatedStyle(() =&gt; ({    transform: [{ rotate: `${rotation.value}deg` }]  }));   return (    &lt;Marker coordinate={coordinate} anchor={{ x: 0.5, y: 0.5 }}&gt;      &lt;View style={styles.markerOuter}&gt;        &lt;Animated.View style={[styles.arrow, arrowStyle]}&gt;          &lt;Icon name=&apos;navigation&apos; color=&apos;#00E5FF&apos; size={20} /&gt;        &lt;/Animated.View&gt;      &lt;/View&gt;    &lt;/Marker&gt;  );}9.3  Cleaning Coverage PolygonAs the robot moves, its GPS trail is recorded. A convex hull polygon is computed from the trail points using Turf.js. This polygon is drawn on the map as a semi-transparent fill to show the area that has been covered.// src/screens/Map/CoverageOverlay.tsximport * as turf from &apos;@turf/turf&apos;; export function CoverageOverlay({ trailCoordinates }) {  const polygon = useMemo(() =&gt; {    if (trailCoordinates.length &lt; 4) return null;    const points = turf.featureCollection(      trailCoordinates.map(c =&gt; turf.point([c.longitude, c.latitude]))    );    const hull = turf.convex(points);    if (!hull) return null;    return hull.geometry.coordinates[0].map(([lng, lat]) =&gt;      ({ latitude: lat, longitude: lng })    );  }, [trailCoordinates]);   if (!polygon) return null;  return (    &lt;Polygon coordinates={polygon}      fillColor=&apos;#00E5FF22&apos;      strokeColor=&apos;#00E5FF66&apos;      strokeWidth={2} /&gt;  );}SCREEN 5Camera &amp; AI Detection ScreenVisionCamera Â· TFLite Â· Skia Overlay Â· Detection HUDT-018   Camera &amp; AI Detection ScreenPriority: HIGH      Effort: 3 days      Libraries: react-native-vision-camera Â· react-native-fast-tflite Â· @shopify/react-native-skia10.1  Camera SetupVision Camera 4 uses a hook-based API. The camera runs at 30fps by default. The frame processor runs on a separate C++ thread and never blocks the JS thread.// src/screens/Camera/CameraScreen.tsximport { Camera, useCameraDevice, useFrameProcessor }  from &apos;react-native-vision-camera&apos;; export function CameraScreen() {  const device = useCameraDevice(&apos;back&apos;);  const [detections, setDetections] = useState([]);   const frameProcessor = useFrameProcessor((frame) =&gt; {    &apos;worklet&apos;;    const results = detectTrash(frame);   // TFLite inference    runOnJS(setDetections)(results);       // pass to JS thread for display  }, []);   if (!device) return &lt;Text&gt;No camera available&lt;/Text&gt;;   return (    &lt;View style={StyleSheet.absoluteFill}&gt;      &lt;Camera device={device} isActive style={StyleSheet.absoluteFill}        frameProcessor={frameProcessor} /&gt;      &lt;DetectionCanvas detections={detections} /&gt;      &lt;DetectionHUD count={detections.length} /&gt;      &lt;CameraControls /&gt;    &lt;/View&gt;  );}10.2  Detection Canvas â€” Skia OverlayThe DetectionCanvas is a react-native-skia Canvas component positioned over the camera view with position: absolute and matching dimensions. For each detection, it draws a bounding box and label.Class colour mapping:Prop / StateType / ValuePurposePlastic#00E5FF  cyanMost common â€” use primary brand colourBottle#FFA94D  orangeSecond most commonDebris#FF6B6B  redDebris and general wasteFoam#A78BFA  purpleFoam and polystyreneOrganic#34D399  greenOrganic waste â€” least hazardous// src/screens/Camera/DetectionCanvas.tsximport { Canvas, RoundedRect, Text as SkiaText,  useFont, Fill } from &apos;@shopify/react-native-skia&apos;; const CLASS_COLORS = {  Plastic: &apos;#00E5FF&apos;, Bottle: &apos;#FFA94D&apos;, Debris: &apos;#FF6B6B&apos;,  Foam: &apos;#A78BFA&apos;, Organic: &apos;#34D399&apos;,}; export function DetectionCanvas({ detections, width, height }) {  const font = useFont(require(&apos;@/assets/fonts/JetBrainsMono.ttf&apos;), 12);   return (    &lt;Canvas style={{ position: &apos;absolute&apos;, width, height }}&gt;      {detections.map((det, i) =&gt; {        const color = CLASS_COLORS[det.label] || &apos;#FFFFFF&apos;;        return (          &lt;React.Fragment key={i}&gt;            {/* Bounding box */}            &lt;RoundedRect x={det.x} y={det.y} width={det.w} height={det.h}              r={6} color={color} style=&apos;stroke&apos; strokeWidth={2} /&gt;             {/* Label chip background */}            &lt;RoundedRect x={det.x} y={det.y - 24} width={det.w * 0.7}              height={20} r={4} color={color} style=&apos;fill&apos; opacity={0.85} /&gt;             {/* Label text */}            &lt;SkiaText x={det.x + 6} y={det.y - 9} text={`${det.label} ${det.confidence}%`}              font={font} color=&apos;#000000&apos; /&gt;          &lt;/React.Fragment&gt;        );      })}    &lt;/Canvas&gt;  );}SCREEN 6Alerts ScreenFlashList Â· Swipe Dismiss Â· Filter Â· Alert DetailT-019   Alerts ScreenPriority: MEDIUM      Effort: 2 days      Libraries: @shopify/flash-list Â· react-native-gesture-handler Â· @gorhom/bottom-sheet11.1  Grouped Alert ListAlerts are grouped by date into sections: Today, Yesterday, and Earlier. FlashList supports sections via the data structure format.// src/screens/Alerts/AlertsScreen.tsxfunction buildSections(alerts) {  const today = format(new Date(), &apos;yyyy-MM-dd&apos;);  const yesterday = format(subDays(new Date(), 1), &apos;yyyy-MM-dd&apos;);  const groups = { Today: [], Yesterday: [], Earlier: [] };   alerts.forEach(alert =&gt; {    const day = format(new Date(alert.timestamp), &apos;yyyy-MM-dd&apos;);    if (day === today) groups.Today.push(alert);    else if (day === yesterday) groups.Yesterday.push(alert);    else groups.Earlier.push(alert);  });   return Object.entries(groups)    .filter(([, items]) =&gt; items.length &gt; 0)    .map(([title, data]) =&gt; ({ title, data }));}11.2  Swipe-to-DismissEach AlertItem is wrapped in a Swipeable from react-native-gesture-handler. Swiping left reveals a red delete action. When the delete action is tapped, the row animates out using Reanimated Layout.// src/screens/Alerts/AlertItem.tsximport { Swipeable } from &apos;react-native-gesture-handler&apos;;import Animated, { FadeOutLeft } from &apos;react-native-reanimated&apos;; export function AlertItem({ alert, onDismiss }) {  const renderRight = () =&gt; (    &lt;Pressable style={styles.deleteAction} onPress={() =&gt; onDismiss(alert.id)}&gt;      &lt;Icon name=&apos;trash&apos; color=&apos;#FFFFFF&apos; /&gt;    &lt;/Pressable&gt;  );   return (    &lt;Animated.View exiting={FadeOutLeft.duration(300)}&gt;      &lt;Swipeable renderRightActions={renderRight}&gt;        &lt;Pressable style={styles.row} onPress={() =&gt; openDetail(alert)}&gt;          &lt;View style={[styles.dot, { backgroundColor: severityColor(alert.severity) }]} /&gt;          &lt;View style={styles.content}&gt;            &lt;Text variant=&apos;body&apos;&gt;{alert.message}&lt;/Text&gt;            &lt;Text variant=&apos;caption&apos; color=&apos;textMuted&apos;&gt;              Robot {alert.robotId}  Â·  {formatDistanceToNow(alert.timestamp)} ago            &lt;/Text&gt;          &lt;/View&gt;          {!alert.read &amp;&amp; &lt;View style={styles.unreadDot} /&gt;}        &lt;/Pressable&gt;      &lt;/Swipeable&gt;    &lt;/Animated.View&gt;  );}SCREEN 7Fleet Management ScreenGrid/List Toggle Â· Multi-Select Â· Batch CommandsT-020   Fleet Management ScreenPriority: MEDIUM      Effort: 2 days      Libraries: @shopify/flash-list Â· react-native-reanimated12.1  Grid and List ToggleThe Fleet screen has a toggle in the navigation header to switch between grid view (2 columns) and list view (1 column). The toggle is stored in the uiStore. The change is animated â€” layout transitions use Reanimated Layout animation.Grid view (2 columns):Each cell is a compact RobotStatusCard showing: name, status dot, battery %, current task.Cards have a minimum height of 120px. Battery is shown as a small horizontal bar rather than the arc gauge.List view (1 column):Each row is a wider card showing more info: name, status, battery bar, signal bars, last GPS position, current task.Chevron on the right to drill into robot detail screen.12.2  Multi-Select ModeLong-pressing any robot card activates multi-select mode. A visual selection indicator appears on tapped cards. A batch command bar slides up from the bottom.// src/screens/Fleet/FleetScreen.tsxconst [selected, setSelected] = useState(new Set());const [multiSelect, setMultiSelect] = useState(false); function handleLongPress(robotId) {  setMultiSelect(true);  setSelected(new Set([robotId]));  HapticFeedback.trigger(&apos;impactMedium&apos;);} function handlePress(robotId) {  if (!multiSelect) { navigateToRobot(robotId); return; }  setSelected(prev =&gt; {    const next = new Set(prev);    next.has(robotId) ? next.delete(robotId) : next.add(robotId);    return next;  });} function sendBatchCommand(command) {  selected.forEach(id =&gt; wsService.send(&apos;robot:command&apos;, { robotId: id, command }));  setMultiSelect(false);  setSelected(new Set());}SCREEN 8Settings ScreenConnection Â· Notifications Â· Security Â· BiometricsT-021   Settings ScreenPriority: LOW      Effort: 2 days      Libraries: expo-secure-store Â· expo-local-authentication Â· react-native-mmkv13.1  Settings ArchitectureSettings are organised into sections rendered as a scrollable list of grouped rows. Each setting row has a label, an optional description, and a control (toggle, text input, or disclosure arrow).The 5 Setting Sections:Connection â€” WebSocket URL, MQTT Broker, Robot ID. Changing any field triggers automatic reconnect.Notifications â€” Toggle switches for 4 alert types: Battery Low, Obstacle Detected, Sensor Failure, Mission Complete.Display â€” Theme toggle (dark/light), chart refresh rate (500ms / 1s / 2s), map style preference.Security â€” Biometric lock toggle, session timeout selector (15min / 30min / 1hr / Never).About â€” App version, build number, a Diagnostics button that shows connection status and latency.13.2  Biometric LockWhen the biometric lock setting is enabled, the app prompts authentication whenever the app returns to the foreground after being backgrounded for more than 30 seconds. The app does not navigate to a lock screen â€” instead it shows a modal overlay over whatever screen the user was on.// src/services/auth/BiometricGuard.tsximport * as LocalAuthentication from &apos;expo-local-authentication&apos;; export function useBiometricGuard() {  const [locked, setLocked] = useState(false);  const backgroundTime = useRef(null);   useEffect(() =&gt; {    const sub = AppState.addEventListener(&apos;change&apos;, async (state) =&gt; {      if (state === &apos;background&apos;) {        backgroundTime.current = Date.now();      }      if (state === &apos;active&apos; &amp;&amp; backgroundTime.current) {        const elapsed = Date.now() - backgroundTime.current;        const timeout = getSettings().sessionTimeout; // ms from settings        if (elapsed &gt; timeout) setLocked(true);      }    });    return () =&gt; sub.remove();  }, []);   async function authenticate() {    const result = await LocalAuthentication.authenticateAsync({      promptMessage: &apos;Unlock Hydrobot&apos;,      fallbackLabel: &apos;Use Passcode&apos;,    });    if (result.success) setLocked(false);  }   return { locked, authenticate };}Complete UI Task ReferenceAll 17 UI tasks from both phases in order of implementation. Complete every row before moving to Phase 3.IDTaskSectionEffortPriorityT-005Atomic Base ComponentsPhase 2 â€” Atoms3 daysCRITICALT-006Robot Status CardPhase 2 â€” Molecules2 daysCRITICALT-007Sensor Data ChartsPhase 2 â€” Charts4 daysCRITICALT-008Haptic Joystick ControllerPhase 2 â€” Controls3 daysHIGHT-009Toast &amp; Alert ComponentsPhase 2 â€” Notifications2 daysHIGHT-014Dashboard ScreenPhase 4 â€” Screen3 daysCRITICALT-015Robot Control ScreenPhase 4 â€” Screen4 daysCRITICALT-016Water Analytics ScreenPhase 4 â€” Screen3 daysHIGHT-017Live Map &amp; GPS ScreenPhase 4 â€” Screen4 daysHIGHT-018Camera &amp; AI ScreenPhase 4 â€” Screen3 daysHIGHT-019Alerts ScreenPhase 4 â€” Screen2 daysMEDIUMT-020Fleet Management ScreenPhase 4 â€” Screen2 daysMEDIUMT-021Settings ScreenPhase 4 â€” Screen2 daysLOWTotal UI effort: 33 days across 2 phases. Phase 2 (14 days) must be fully complete before beginning Phase 4. All components in Phase 2 are directly reused inside the Phase 4 screens.
