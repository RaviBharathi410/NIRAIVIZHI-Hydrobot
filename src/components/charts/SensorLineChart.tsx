import React from 'react';

interface SensorLineChartProps {
    data: any[];
    color: string;
    label?: string;
}

export declare function SensorLineChart(props: SensorLineChartProps): JSX.Element;
export default SensorLineChart;
